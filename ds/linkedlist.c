#include <stdio.h>

typedef struct listnode Listnode;
struct listnode{
    struct listnode prev;
    struct listnode next; 
    int *data;
    int search(int key)
    
};

int main(int argc, char *argv[]){
    
}